<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Runtime-requested on Android 13+. Used only for local hazard alerts. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!--
      No INTERNET permission in this base manifest by design: all inference is
      on-device and the `offline` flavor must run in Airplane Mode. Only the
      `connected` flavor adds INTERNET via src/connected/AndroidManifest.xml,
      solely for edge->cloud reporting; detection never depends on it.
    -->

    <application
        android:name=".GuardianApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Guardian">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Guardian">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
