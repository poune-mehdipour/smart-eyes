package com.axior.guardian.ui.theme

import androidx.compose.material3.Typography

// Default Material 3 typography for now; a branded type scale can replace this
// without touching call sites.
val GuardianTypography = Typography()
